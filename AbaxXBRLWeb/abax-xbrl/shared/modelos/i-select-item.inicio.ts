module abaxXBRL.shared.modelos {

    /**
    * Interface que define los elementos de un campo select.
    **/
    export interface ISelectItem {

        /** Etiqueta del elemento */
        Etiqueta: string;

        /** Valor del elemento */
        Valor: any;
    }
}